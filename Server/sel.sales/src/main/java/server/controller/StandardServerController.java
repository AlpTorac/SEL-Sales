package server.controller;

import server.model.IServerModel;

public class StandardServerController extends ServerController {
	public StandardServerController(IServerModel model) {
		super(model);
	}
}
